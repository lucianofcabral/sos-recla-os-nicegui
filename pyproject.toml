[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sos-reclamos-nicegui"
version = "0.1.0"
description = "Gestión de reclamos para SOS"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "bcrypt>=5.0.0",
    "nicegui>=3.16.0",
    "openpyxl>=3.1.5",
    "psycopg[binary]>=3.3.4",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.2",
    "sqlmodel>=0.0.39",
]

[tool.hatch.build.targets.wheel]
packages = ["src"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.15.20",
    "testcontainers[postgres]>=4.15.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 88
target-version = "py313"  # ajustá según tu versión de Python

# Directorios/archivos a excluir
exclude = [
    ".git",
    ".venv",
    "venv",
    "__pycache__",
    "build",
    "dist",
    "migrations",
]

[tool.ruff.lint]
select = [
    "E",      # pycodestyle errors
    "W",      # pycodestyle warnings
    "F",      # pyflakes
    "I",      # isort (orden de imports)
    "N",      # pep8-naming
    "UP",     # pyupgrade (sintaxis moderna)
    "B",      # flake8-bugbear (bugs comunes)
    "C4",     # flake8-comprehensions
    "SIM",    # flake8-simplify
    "RUF",    # reglas propias de Ruff
]

ignore = [
    "E501",   # línea muy larga (si ya controlás con line-length)
]

# Permitir auto-fix
fixable = ["ALL"]
unfixable = []

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]  # imports no usados permitidos en __init__
"tests/*" = ["S101"]      # permitir asserts en tests

[tool.ruff.format]
quote-style = "single"
skip-magic-trailing-comma = false
line-ending = "auto"
